// top nav
nav.ui.top.fixed.inverted.menu
    .left.menu
        a.sidebar-menu-toggler.item(href="#", data-target="#sidebar")
            i.sidebar.icon
        a.header.item(href="#") #{ bundles[lang]['app.name'] }
    .right.menu
        a.item(href="#")
            i.bell.icon
        // language selection
        .ui.dropdown.item
            i.globe.icon
            #languages.menu
                each lang in supportedLanguages
                    button.item(data-lang=lang.getKey()) #{ lang.getValue() }
        .ui.dropdown.item
            i.user.cirlce.icon
            .menu
                a.item(href="/profile")
                    i.info.circle.icon
                    |
                    | #{ bundles[lang]['profile'] }
                a.item(href="/settings")
                    i.wrench.icon
                    |
                    | #{ bundles[lang]['settings'] }
                buttom#logout.item
                    i.sign.out.icon
                    |
                    | #{ bundles[lang]['logout'] }
// top nav
